# Gin-Demo 后端设计文档

## 项目概述
这是一个基于 Gin 框架开发的 Todo 应用后端服务，采用清晰的分层架构设计，实现了用户管理和待办事项管理等功能。

## 技术栈
- **框架**: Gin (Web框架)
- **数据库**: MySQL (主数据库，使用 XORM 作为 ORM)
- **缓存**: Redis (用于 Token 黑名单等功能)
- **认证**: JWT (JSON Web Token)
- **开发语言**: Go

## 系统架构

### 目录结构
```
backend/
├── cmd/                # 主程序入口
├── config/            # 配置文件
├── internal/          # 内部包
│   ├── common/        # 公共组件
│   ├── handler/       # HTTP 处理器
│   ├── middleware/    # 中间件
│   ├── model/         # 数据模型
│   ├── repository/    # 数据访问层
│   ├── route/         # 路由定义
│   └── service/       # 业务逻辑层
├── pkg/               # 公共工具包
├── resources/         # 资源文件
├── scripts/           # 脚本文件
├── test/             # 测试文件
└── uploads/          # 文件上传目录
```

### 分层架构
1. **Handler 层**: 处理 HTTP 请求，参数验证，响应封装
2. **Service 层**: 实现核心业务逻辑
3. **Repository 层**: 数据访问层，处理数据库操作
4. **Model 层**: 定义数据模型和数据库表结构

## 核心功能

### 1. 用户管理
- **用户注册**
  - 用户名和密码验证
  - 密码加密存储
  - 支持角色分配
  - 支持昵称和邮箱信息
  
- **用户登录**
  - JWT token 认证
  - Token 过期管理
  - Token 黑名单功能
  
- **用户信息**
  - 获取用户信息
  - 更新用户资料
  - 头像上传功能

### 2. Todo 管理
- **Todo 项操作**
  - 创建待办事项
  - 查看待办事项列表
  - 获取待办事项详情
  - 更新待办事项
  - 删除待办事项
  - 更改待办事项状态

### 3. 系统功能
- **配置管理**
  - 基于 INI 文件的配置系统
  - 支持多环境配置
  - 数据库配置
  - Redis 配置
  - 服务器配置
  - JWT 配置
  - CORS 配置
  - 日志配置

- **中间件**
  - CORS 跨域处理
  - 错误处理中间件
  - 认证中间件
  - 请求日志记录

- **数据库设计**
  - 用户表设计
  - Todo 表设计
  - 使用 XORM 进行数据库操作
  - 数据库连接池管理

- **缓存设计**
  - Redis 连接池管理
  - Token 黑名单管理
  - 缓存策略

## 安全特性
1. **认证安全**
   - 密码加密存储
   - JWT token 认证
   - Token 过期机制
   - Token 黑名单

2. **请求安全**
   - CORS 安全配置
   - 请求参数验证
   - 错误处理和日志记录

3. **数据安全**
   - 数据库连接池管理
   - 事务支持
   - 参数化查询

## 性能优化
1. **数据库优化**
   - 连接池配置
   - 最大连接数限制
   - 空闲连接管理

2. **Redis优化**
   - 连接池设置
   - 最小空闲连接
   - 连接超时配置
   - 空闲连接检查

3. **服务器优化**
   - 优雅关闭
   - 并发控制
   - 超时处理

## 开发和部署
1. **开发模式**
   - 详细的错误信息
   - SQL 语句日志
   - 调试信息输出

2. **生产模式**
   - 精简的错误响应
   - 优化的日志级别
   - 安全的错误处理

## API 响应规范
```json
{
    "code": 200,
    "message": "success",
    "data": {},
    "error": ""
}
```

## 错误处理
- 400: 请求参数错误
- 401: 未授权
- 403: 禁止访问
- 404: 资源不存在
- 500: 服务器内部错误

## 文件上传
- 支持头像上传
- 文件类型验证
- 文件大小限制
- 安全的文件存储 